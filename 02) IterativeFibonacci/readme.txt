IterativeFibonacci

iterfibo() 함수 구현

 피보나치 수열 정의에 의하면 이 수열을 f라 할 때 f(0)과 f(1)은 값을 직접 지정해놔야 합니다. 이 값들을 일단 FList에 넣어둡니다.
 앞 두수를 더한 것이 피보나치수이기 때문에 리스트길이가 n+1이 될때까지(0이 포함돼있기 때문) 반복하여 FList에서 인덱스가 -1, -2인 값을 더하여 FList에 추가해줍니다.
 그리고 while문을 벗어나면 그 리스트의 최종값을 리턴합니다.
